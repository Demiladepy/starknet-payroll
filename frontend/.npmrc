# get-starknet-core (used by @starknet-react/core) declares peer starknet@^5.18.0
# while we use starknet@^6 for the app. Ignore peer conflict; starknet 6 works with current Starknet React.
legacy-peer-deps=true
